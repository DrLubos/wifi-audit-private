# credentials and per-sensor configuration
.env
sensor.conf
collector.conf

# python
__pycache__/
*.pyc

# node / vite (server/frontend)
node_modules/
dist/

# local data
*.db
*.db-wal
*.db-shm

# macOS
.DS_Store

# database dumps (server/backup.sh)
backups/
*.dump

# staged-attack evaluation artefacts and generated output (server/docs)
# committed only when a specific run is archived for the thesis
server/docs/evaluation.md
server/docs/evaluation_trials.csv
server/docs/groundtruth*.csv
server/docs/counter_only.json
server/docs/cap/
*.pcap
